# `data/processed/`（本機專用，不進公開 git）

將你**具合法授權**、且已整理為球季面板之 `*_metrics.csv` 放在此目錄。

- 檔名與欄位說明見上一層 [`data/README.md`](../README.md) 與專案根目錄 [`schema.json`](../../schema.json)。  
- 此目錄內 `*.csv` 已列入 `.gitignore`，避免付費資料外洩。  
- Clone 公開 repo 後此處通常為空；請自行放入資料，或依賴 `sample_data/processed/` 的合成示範。
